import { To4PrecisionPipe } from './to4-precision.pipe';

describe('To4PrecisionPipe', () => {
  it('create an instance', () => {
    const pipe = new To4PrecisionPipe();
    expect(pipe).toBeTruthy();
  });
});
