import { MinifyPathPipe } from './minify-path.pipe';

describe('MinifyPathPipe', () => {
  it('create an instance', () => {
    const pipe = new MinifyPathPipe();
    expect(pipe).toBeTruthy();
  });
});
