import { ExtendedDirectory } from './extended-directory';

describe('ExtendedDirectory', () => {
  it('should create an instance', () => {
    expect(new ExtendedDirectory()).toBeTruthy();
  });
});
